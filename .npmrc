# Pin this repo to the public npm registry.
#
# Without this, `npm install` inherits whatever registry the machine is
# configured for. A private/corporate registry ends up baked into every
# `resolved` URL in package-lock.json, which then breaks Docker builds, CI, and
# anyone who clones the project.
registry=https://registry.npmjs.org/
